# URL to download the violation dataset CSV on startup.
# Required by the backend when the CSV file is not present locally
# (i.e. in Railway/Vercel deployments where the large file isn't in git).
# Upload the CSV to any public or signed URL and set this to that URL.
DATASET_URL=
